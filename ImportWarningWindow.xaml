<Window x:Class="FattureViewer.ImportWarningWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Controllo importazione"
        Width="980"
        Height="620"
        MinWidth="820"
        MinHeight="480"
        WindowStartupLocation="CenterOwner"
        Background="#F5F6F8"
        FontFamily="Segoe UI"
        Icon="logo/logo50.png">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Controlla le fatture prima di importare"
                       FontSize="22"
                       FontWeight="SemiBold"
                       Foreground="#9A6700"/>
            <TextBlock x:Name="WarningText"
                       Margin="0,8,0,0"
                       FontSize="15"
                       FontWeight="SemiBold"
                       TextWrapping="Wrap"/>
        </StackPanel>

        <Border Grid.Row="1"
                Margin="0,14,0,12"
                Padding="12"
                Background="#FFF3CD"
                BorderBrush="#E3C46B"
                BorderThickness="1"
                CornerRadius="4">
            <TextBlock Text="Le righe gialle sono sospette. Apri una fattura per verificarla: le spunte non cambiano quando chiudi l’anteprima. “Importa selezionate” ignora ogni fattura non spuntata."
                       TextWrapping="Wrap"
                       Foreground="#6B4C00"/>
        </Border>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Items}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserReorderColumns="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  RowHeaderWidth="0"
                  AlternatingRowBackground="#FAFAFA"
                  Background="White"
                  BorderBrush="#CCCCCC"
                  BorderThickness="1"
                  SelectionMode="Single"
                  EnableRowVirtualization="True"
                  EnableColumnVirtualization="True">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsSuspicious}"
                                     Value="True">
                            <Setter Property="Background"
                                    Value="#FFF3CD"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Importa" Width="75">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      ToolTip="Seleziona questa fattura"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Controllo"
                                    Binding="{Binding Status}"
                                    Width="95"
                                    IsReadOnly="True"/>
                <DataGridTextColumn Header="Mittente"
                                    Binding="{Binding Invoice.SenderName}"
                                    Width="*"
                                    IsReadOnly="True"/>
                <DataGridTextColumn Header="P.IVA mittente"
                                    Binding="{Binding Invoice.SenderVatNumber}"
                                    Width="145"
                                    IsReadOnly="True"/>
                <DataGridTextColumn Header="Numero fattura"
                                    Binding="{Binding Invoice.InvoiceNumber}"
                                    Width="140"
                                    IsReadOnly="True"/>
                <DataGridTextColumn Header="Data"
                                    Binding="{Binding Invoice.Date, StringFormat={}{0:dd/MM/yyyy}}"
                                    Width="105"
                                    IsReadOnly="True"/>
                <DataGridTemplateColumn Header="Anteprima" Width="125">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Apri fattura"
                                    Tag="{Binding}"
                                    Padding="8,4"
                                    Margin="4"
                                    Click="PreviewButton_Click"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="3" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="SelectionText"
                       VerticalAlignment="Center"
                       Foreground="#555"/>
            <StackPanel Grid.Column="1"
                        Orientation="Horizontal">
                <Button Content="Annulla"
                        Width="105"
                        Padding="10,7"
                        Margin="0,0,10,0"
                        IsCancel="True"
                        Click="CancelButton_Click"/>
                <Button x:Name="ImportSelectedButton"
                        Content="Importa selezionate"
                        Width="165"
                        Padding="10,7"
                        Margin="0,0,10,0"
                        Click="ImportSelectedButton_Click"/>
                <Button Content="Importa tutte"
                        Width="135"
                        Padding="10,7"
                        Background="#0078D7"
                        Foreground="White"
                        FontWeight="SemiBold"
                        IsDefault="True"
                        Click="ImportAllButton_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
