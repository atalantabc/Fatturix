<Window x:Class="FattureViewer.ImportPeriodWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Periodo Importazione" Height="210" Width="340" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" Background="#F5F6F8">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Periodo di riferimento" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,12"/>

        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Anno" VerticalAlignment="Center"/>
            <TextBox x:Name="YearBox" Grid.Column="1" Height="28" VerticalContentAlignment="Center"/>
        </Grid>

        <Grid Grid.Row="2" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Mese" VerticalAlignment="Center"/>
            <TextBox x:Name="MonthBox" Grid.Column="1" Height="28" VerticalContentAlignment="Center"/>
        </Grid>

        <TextBlock x:Name="ErrorText" Grid.Row="3" Foreground="#B00020" Visibility="Collapsed" TextWrapping="Wrap"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Annulla" Width="80" Margin="0,0,8,0" Click="CancelButton_Click"/>
            <Button Content="Importa" Width="90" IsDefault="True" Click="OkButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
