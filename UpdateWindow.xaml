<Window x:Class="FattureViewer.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Aggiornamento disponibile"
        Width="560"
        Height="430"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#F5F6F8"
        FontFamily="Segoe UI"
        Icon="logo/logo100.png"
        Closed="Window_Closed">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="È disponibile un aggiornamento"
                   FontSize="22"
                   FontWeight="SemiBold"
                   Foreground="#222222"/>

        <TextBlock x:Name="VersionText"
                   Grid.Row="1"
                   Margin="0,6,0,14"
                   FontSize="14"
                   Foreground="#555555"/>

        <Border Grid.Row="2"
                Background="White"
                BorderBrush="#D8D8D8"
                BorderThickness="1"
                CornerRadius="4"
                Padding="12">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="ReleaseNotesText"
                           TextWrapping="Wrap"
                           FontSize="13"
                           Foreground="#333333"/>
            </ScrollViewer>
        </Border>

        <StackPanel x:Name="ProgressPanel"
                    Grid.Row="3"
                    Margin="0,14,0,0"
                    Visibility="Collapsed">
            <TextBlock x:Name="StatusText"
                       Margin="0,0,0,6"
                       Foreground="#444444"/>
            <ProgressBar x:Name="DownloadProgress"
                         Height="18"
                         Minimum="0"
                         Maximum="100"/>
        </StackPanel>

        <StackPanel Grid.Row="4"
                    Margin="0,16,0,0"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button x:Name="CancelButton"
                    Content="Non ora"
                    Width="100"
                    Padding="10,6"
                    Margin="0,0,8,0"
                    Click="CancelButton_Click"/>
            <Button x:Name="InstallButton"
                    Content="Sì, aggiorna"
                    Width="120"
                    Padding="10,6"
                    Background="#0078D7"
                    Foreground="White"
                    FontWeight="SemiBold"
                    Click="InstallButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
